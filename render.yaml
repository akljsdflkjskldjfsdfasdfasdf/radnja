# render.yaml — uputstvo za Render (hosting): kako da izgradi i pokrene aplikaciju.
services:
  - type: web
    name: radnja
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
